id: disabled_button
info:
  description: Finds disabled HTML buttons, which might be bypassable or hide functionality.
  author: sysrekt
  risk_score: 2.0
  tags:
  - legacy
matchers:
- type: regex
  part: body
  regex:
  - <button[^>]*disabled[^>]*>
  case_insensitive: false
