id: jsonp_callback
info:
  description: Detects JSONP callback patterns, a relic from pre-CORS APIs.
  author: sysrekt
  risk_score: 2.0
  tags:
  - legacy
matchers:
- type: regex
  part: url
  regex:
  - '[?&](callback|jsonp|cb|json_callback)='
  case_insensitive: true
