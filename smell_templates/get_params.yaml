id: get_params
info:
  description: Detects URLs with GET parameters, which are primary injection points
    for SQLi, XSS, etc.
  author: sysrekt
  risk_score: 1.0
matchers:
- type: regex
  part: url
  regex:
  - \?.*=
  case_insensitive: true
