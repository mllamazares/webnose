id: vintage_DOCTYPE
info:
  description: Identifies older DOCTYPE declarations (e.g., HTML 4.01), suggesting
    legacy applications.
  author: sysrekt
  risk_score: 1.0
matchers:
- type: regex
  part: body
  regex:
  - <!DOCTYPE\s+HTML\s+PUBLIC
  case_insensitive: false
