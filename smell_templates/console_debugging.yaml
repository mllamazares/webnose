id: console_debugging
info:
  description: Detects 'console.log' or other console debugging statements left in
    production code.
  author: sysrekt
  risk_score: 2.0
matchers:
- type: regex
  part: body
  regex:
  - console\.(log|warn|error|info|debug)
  case_insensitive: true
