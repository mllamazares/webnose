id: plaintext_passwords
info:
  description: Detects password fields with insecure attributes.
  author: sysrekt
  risk_score: 5.0
  tags:
  - security
matchers:
- type: regex
  part: body
  regex:
  - type=[\"']password[\"'](?!.*autocomplete=[\"']off[\"'])
  case_insensitive: true
