id: session_id_in_url
info:
  description: Flags session identifiers embedded in URLs.
  author: sysrekt
  risk_score: 5.0
  tags:
  - security
matchers:
- type: regex
  part: url
  regex:
  - (PHPSESSID|JSESSIONID|ASPSESSIONID|SID|CFID|CFTOKEN)=
  - session_id=
  case_insensitive: true
